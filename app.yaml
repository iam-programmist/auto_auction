runtime: python311
entrypoint: python bot.py

handlers:
- url: /.*
  script: auto

env_variables:
  DJANGO_SETTINGS_MODULE: server.settings

basic_scaling:
  max_instances: 1
  idle_timeout: 10m

instance_class: F2